{% extends "base.html.twig" %}

{% block main %}
<div class="page-header content-box">
    <h1>Kortlek</h1>
</div>



<div class="content-box">
    <div class="card-links">
        <a href="{{ path('deck') }}" class="button">Se kortleken</a>
        <a href="{{ path('shuffle_deck') }}" class="button">Blanda kortleken</a>
        <a href="{{ path('draw') }}" class="button">Dra kort</a>
        <a href="{{ path('twenty_one') }}" class="button">Kortspelet Tjugoett</a>
    </div>
</div>

{% endblock %}
